# Terraform Backend Configuration
# This file contains the S3 backend configuration for state management
# Update these values with your actual AWS account details

bucket         = "wiz-tf-state-491085396284"
key            = "wiz-exercise/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "wiz-tf-lock"
encrypt        = true

# To use this configuration:
# terraform init -backend-config=backend.conf